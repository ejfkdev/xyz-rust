[package]
name = "xyz-clap"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]
xyz-rust = { path = "../.." }
serde = { version = "1", features = ["derive"] }
clap = { version = "4", features = ["string"] }